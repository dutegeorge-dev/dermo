{
  "name": "bars-site",
  "version": "1.0.0",
  "description": "Каркас сайта ООО ТЛК БАРС — 11ty + Tailwind + TypeScript",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "clean": "rimraf _site",
    "build:css": "postcss src/assets/css/tailwind.css -o _site/assets/css/styles.css",
    "build:js": "esbuild src/assets/ts/main.ts --bundle --format=iife --target=es2018 --outfile=_site/assets/js/main.js",
    "build:js:prod": "npm run build:js -- --minify",
    "build:11ty": "node --import tsx ./node_modules/@11ty/eleventy/cmd.cjs --config=eleventy.config.ts",
    "build": "npm run clean && cross-env NODE_ENV=production npm run build:css && cross-env NODE_ENV=production npm run build:js:prod && cross-env NODE_ENV=production npm run build:11ty",
    "watch:css": "postcss src/assets/css/tailwind.css -o _site/assets/css/styles.css --watch",
    "watch:js": "npm run build:js -- --watch",
    "watch:11ty": "node --import tsx ./node_modules/@11ty/eleventy/cmd.cjs --config=eleventy.config.ts --serve",
    "server": "node --import tsx server/index.ts",
    "watch:server": "node --import tsx --watch server/index.ts",
    "dev": "npm run build:css && npm run build:js && concurrently --kill-others-on-fail \"npm:watch:css\" \"npm:watch:js\" \"npm:watch:11ty\" \"npm:watch:server\"",
    "preview": "npm run build && npx http-server _site -p 8080 -c-1",
    "preview:fast": "npm run clean && npm run build:css && npm run build:js && npm run build:11ty && npx http-server _site -p 8080 -c-1",
    "cms": "decap-server",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "eleventy",
    "11ty",
    "tailwind",
    "typescript",
    "static-site"
  ],
  "author": "ООО ТЛК БАРС",
  "license": "UNLICENSED",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-img": "^6.0.4",
    "@types/node": "^22.10.0",
    "autoprefixer": "^10.4.20",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "cssnano": "^7.0.6",
    "decap-server": "^3.9.1",
    "esbuild": "^0.24.2",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.2.0",
    "markdown-it-anchor": "^9.2.0",
    "postcss": "^8.4.49",
    "postcss-cli": "^11.0.0",
    "rimraf": "^6.0.1",
    "tailwindcss": "^3.4.17",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
