/**
 * Конфигурация калькулятора доставки для вёрстки.
 *
 * Значения не дублируются: тарифы, лимит 300 кг/куб.м, список городов и ставки
 * НДС описаны один раз в `server/calc.ts` — тем же модулем пользуется бэкенд,
 * когда пересчитывает заявку перед отправкой в Bitrix24. Здесь конфиг только
 * прокидывается в шаблон (`src/pages/calculator.njk`), а оттуда JSON-блоком —
 * клиентскому скрипту (`src/assets/ts/calculator.ts`).
 */

import { CALC_CONFIG } from "../../server/calc.ts";

export default CALC_CONFIG;
