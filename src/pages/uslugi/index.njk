---
permalink: "{{ '/uslugi/' | localizedUrl(listLang) }}"
titleKey: uslugi.index.title
descriptionKey: uslugi.index.description
---
{# Витрина «Услуги» — простой шаблон: H1 + подзаголовок + сетка карточек. #}
{% from "components/link-card.njk" import linkCard %}

<p class="mt-2 max-w-2xl text-base leading-relaxed text-muted">{{ "uslugi.index.subtitle" | t(locale) }}</p>

<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
  {{ linkCard("truck", ("mega.logistics.title" | t(locale)), uslugi.dostavkaUrl | localizedUrl(locale)) }}
  {{ linkCard("store", ("mega.trade.title" | t(locale)), uslugi.torgovlyaUrl | localizedUrl(locale)) }}
</div>
