---
permalink: "{{ '/uslugi/dostavka/goroda/' | localizedUrl(listLang) }}"
titleKey: uslugi.gorodaList.title
descriptionKey: uslugi.gorodaList.description
---
{# Список городов (шаблонный). Карточки строятся из _data/uslugi.ts. #}
{% from "components/link-card.njk" import linkCard %}

<p class="mt-2 max-w-2xl text-base leading-relaxed text-muted">{{ "uslugi.gorodaList.subtitle" | t(locale) }}</p>

<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
  {% for c in uslugi.goroda %}
    {{ linkCard(c.icon, (c.nameKey | t(locale)), c.url | localizedUrl(locale)) }}
  {% endfor %}
</div>
