---
permalink: "{{ '/uslugi/dostavka/sposoby/' | localizedUrl(listLang) }}"
titleKey: uslugi.sposobyList.title
descriptionKey: uslugi.sposobyList.description
---
{# Список способов доставки (шаблонный). Карточки строятся из _data/uslugi.ts. #}
{% from "components/link-card.njk" import linkCard %}

<p class="mt-2 max-w-2xl text-base leading-relaxed text-muted">{{ "uslugi.sposobyList.subtitle" | t(locale) }}</p>

<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
  {% for m in uslugi.methods %}
    {{ linkCard(m.icon, (m.nameKey | t(locale)), m.url | localizedUrl(locale)) }}
  {% endfor %}
</div>
