---
permalink: /uslugi/torgovlya/
titleKey: uslugi.torgovlya.title
descriptionKey: uslugi.torgovlya.description
---
{# Витрина «Торговля» — простой шаблон: H1 + подзаголовок + сетка из 10 услуг. #}
{% from "components/link-card.njk" import linkCard %}

<p class="mt-2 max-w-2xl text-base leading-relaxed text-muted">{{ "uslugi.torgovlya.subtitle" | t(locale) }}</p>

<div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
  {% for s in uslugi.torgovlyaServices %}
    {{ linkCard(s.icon, (s.nameKey | t(locale)), s.url | localizedUrl(locale)) }}
  {% endfor %}
</div>
